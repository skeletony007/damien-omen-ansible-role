dependencies:
  # intel-undervolt
  - role: intel-undervolt
  - role: cpupower

  # ssd
  - role: util-linux # contains fstrim.timer

  # system
  - role: inetutils
  - role: ntp
  # system/graphics
  - role: xf86-video-nouveau
  - role: mesa
  - role: lib32-mesa

  # rsync
  - role: rsync

  # greetd
  - role: greetd
