dependencies:
  # intel-undervolt
  - role: intel-undervolt
  - role: cpupower

  # ssd
  - role: util-linux # contains fstrim.timer

  # system
  - role: inetutils
  - role: ntp

  # rsync
  - role: rsync

  # greetd
  - role: greetd
