dependencies:
  - role: intel-undervolt
  - role: cpupower
  - role: util-linux # contains fstrim.timer (ssd)
  - role: inetutils # contains hostname (system)
  - role: ntp # network time protocol (system)
